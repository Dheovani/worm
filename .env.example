# Driver used by Worm: sqlite, postgresql or mysql.
database_type=sqlite

# Database name or SQLite file path. Use :memory: for an ephemeral SQLite database.
dbname=worm.db

# Required by PostgreSQL and MySQL. Ignored by SQLite.
host=localhost
port=5432
username=worm
password=change-me
